# Description: CI builds the Nuxt client and validates the Botble Laravel admin, then deploys main to the server over SSH.
name: CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-cd-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"
  PNPM_VERSION: "11.7.0"
  PHP_VERSION: "8.3"
  CLIENT_DIR: client
  ADMIN_DIR: admin

jobs:
  client-ci:
    name: Nuxt client
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ${{ env.CLIENT_DIR }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: ${{ env.CLIENT_DIR }}/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Nuxt
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: pnpm run build

  admin-ci:
    name: Laravel admin
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ${{ env.ADMIN_DIR }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: curl, gd, mbstring, pdo, pdo_mysql, pdo_sqlite, sqlite3, zip
          coverage: none

      - name: Validate Composer files
        run: composer validate --no-check-publish

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: ~/.composer/cache/files
          key: composer-${{ runner.os }}-${{ hashFiles('admin/composer.lock') }}
          restore-keys: composer-${{ runner.os }}-

      - name: Prepare environment
        run: cp .env.example .env

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction --no-progress

      - name: Generate app key
        run: php artisan key:generate --force

      - name: Run tests
        run: php artisan test

  deploy:
    name: Deploy production
    runs-on: ubuntu-latest
    needs:
      - client-ci
      - admin-ci
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    environment: production

    steps:
      - name: Deploy over SSH
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_PORT: ${{ secrets.SSH_PORT || '22' }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_KEY }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          BRANCH_NAME: ${{ github.ref_name }}
          PNPM_VERSION: ${{ env.PNPM_VERSION }}
        run: |
          set -euo pipefail

          for required_var in SSH_HOST SSH_PORT SSH_USER SSH_KEY DEPLOY_PATH BRANCH_NAME PNPM_VERSION; do
            if [ -z "${!required_var}" ]; then
              echo "Missing required deploy variable: ${required_var}" >&2
              exit 1
            fi
          done

          echo "Preparing SSH key"
          mkdir -p ~/.ssh
          printf '%s\n' "${SSH_KEY}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

          echo "Adding server host key"
          ssh-keyscan -T 20 -p "${SSH_PORT}" "${SSH_HOST}" >> ~/.ssh/known_hosts

          echo "Starting remote deploy"
          ssh -o BatchMode=yes -o StrictHostKeyChecking=yes -i ~/.ssh/deploy_key -p "${SSH_PORT}" "${SSH_USER}@${SSH_HOST}" "bash -se" <<EOF
          set -euo pipefail

          echo "Deploy path: ${DEPLOY_PATH}"
          cd "${DEPLOY_PATH}"
          echo "Syncing branch: ${BRANCH_NAME}"
          git fetch --prune origin
          git checkout "${BRANCH_NAME}"
          git reset --hard "origin/${BRANCH_NAME}"

          echo "Installing admin dependencies"
          cd admin
          composer install --no-dev --prefer-dist --optimize-autoloader --no-interaction --no-progress
          echo "Optimizing admin"
          php artisan optimize:clear
          php artisan migrate --force
          php artisan storage:link || true
          php artisan cms:publish:assets || true
          php artisan config:cache
          php artisan view:cache

          echo "Installing client dependencies"
          cd ../client
          corepack enable
          corepack prepare "pnpm@${PNPM_VERSION}" --activate
          pnpm install --frozen-lockfile
          echo "Building client"
          NODE_OPTIONS="--max-old-space-size=4096" pnpm run build

          if command -v pm2 >/dev/null 2>&1; then
            echo "Reloading pm2 app"
            pm2 reload hst-client --update-env || pm2 start .output/server/index.mjs --name hst-client --update-env
            pm2 save
          else
            echo "pm2 not found; build completed without process reload"
          fi
          EOF
