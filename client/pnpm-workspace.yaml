# Description: Defines the single Nuxt client package workspace and allowed dependency build scripts.
packages:
  - .

allowBuilds:
  '@parcel/watcher': false
  esbuild: false
  sharp: false
  vue-demi: false
